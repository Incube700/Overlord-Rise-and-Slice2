# Дизайн‑документация: Overlord: Rise and Slice

Этот файл содержит заметки по дизайну игры «Overlord: Rise and Slice» — dark-fantasy Top-Down Exploration Slasher с процедурной прогрессией.

## 🎮 Основная концепция

**Жанр:** Top-Down Action Slasher + Roguelike Elements  
**Вдохновение:** Dead Cells, Hyper Light Drifter, Moonlighter, Dark Souls, Devil May Cry  
**Сеттинг:** Тёмный, мрачный полуразрушенный мир. Игрок — восставший Overlord, который поднимается с 100-го уровня замка к тронному залу (уровень 1), восстанавливая силу, освобождая слуг, разрушая проклятия.  
**Тональность:** Готика, чёрный юмор, грязная магия, агония и стиль.

Игрок управляет персонажем, который должен исследовать полуразрушенный мир, созданный воображением и воспоминаниями. В процессе исследования игрок сталкивается с загадками, врагами и элементами, раскрывающими историю вселенной.

---

## 🏰 Лор и иерархия подземелья

### 📖 **История падения и возрождения**

Когда-то Overlord правил из своего величественного замка, возвышающегося над землёй. Но герои пришли, разрушили его власть и превратили замок в руины. То, что когда-то было троном могущества, стало подземным лабиринтом из 100 уровней, где каждый этаж — это память о былом величии и свидетельство унижения.

**Overlord пробуждается на самом дне — на 100-м уровне.** Здесь, в глубинах забвения, его сила почти исчерпана, а тело едва держится вместе. Но даже в таком состоянии он остаётся Overlord, и его воля к возвращению непоколебима.

### 🗡️ **Иерархия захватчиков: от слабых к сильным**

После падения Overlord, его разрушенный замок стал магнитом для всех видов существ. **Люди, авантюристы, монстры и демоны заселили руины, создав жестокую иерархию силы:**

#### **🏚️ Уровни 90-100: Падальщики и отчаявшиеся**
**Население:** Нищие, беглецы, слабые гоблины, крысы-мутанты, зомби-бродяги  
**Лор:** *"На самом дне живут те, кому некуда больше идти. Здесь правит отчаяние, а единственный закон — выживание любой ценой."*

**Что можно найти:**
- **Сломанное оружие** — ржавые кинжалы, треснувшие мечи
- **Лохмотья** — остатки одежды, дающие минимальную защиту  
- **Крошки еды** — гнилые припасы, дающие 1-2 HP
- **Записки отчаяния** — дневники тех, кто не смог выбраться

**Механика:** Враги слабые (5-10 HP), но многочисленные. Лут редкий и низкокачественный.

#### **⚔️ Уровни 70-89: Мародёры и охотники за сокровищами**
**Население:** Бандиты, авантюристы-неудачники, орки-разведчики, культисты низшего ранга  
**Лор:** *"Здесь обосновались те, кто пришёл за лёгкой наживой. Они грабят слабых внизу и боятся сильных наверху."*

**Что можно найти:**
- **Награбленное оружие** — мечи, топоры, луки среднего качества
- **Кожаные доспехи** — защита, украденная у торговцев
- **Мешки с монетами** — валюта для торговли с призраками-торговцами
- **Карты уровней** — показывают секретные проходы

**Механика:** Враги средней силы (15-25 HP), используют тактику. Лут более ценный.

#### **🛡️ Уровни 40-69: Профессиональные наёмники**
**Население:** Опытные наёмники, паладины-изгои, маги-ренегаты, демоны среднего ранга  
**Лор:** *"Эти уровни контролируют профессионалы. Они пришли не за лёгкой добычей — они знают цену силы и готовы за неё сражаться."*

**Что можно найти:**
- **Зачарованное оружие** — мечи с элементальным уроном
- **Магические доспехи** — броня с сопротивлениями
- **Зелья и свитки** — лечение, бафферы, заклинания
- **Руны силы** — улучшения для оружия и способностей

**Механика:** Сильные враги (30-50 HP) с особыми способностями. Качественный лут.

#### **👑 Уровни 20-39: Лорды войны и командиры**
**Население:** Военачальники, архимаги, демонические лорды, павшие герои  
**Лор:** *"Здесь правят те, кто когда-то командовал армиями. Они превратили эти уровни в свои крепости и не собираются их отдавать."*

**Что можно найти:**
- **Легендарное оружие** — мечи героев, посохи архимагов
- **Артефактные доспехи** — броня с уникальными свойствами
- **Души павших** — валюта для самых мощных улучшений
- **Древние свитки** — заклинания высокого уровня

**Механика:** Боссы и мини-боссы (60-100 HP). Редкий и мощный лут.

#### **⚡ Уровни 1-19: Узурпаторы трона**
**Население:** Герои-завоеватели, архидемоны, божественные посланники, те, кто убил Overlord  
**Лор:** *"На вершине сидят те, кто считает себя новыми хозяевами. Они заняли трон Overlord и думают, что заслужили его. Как же они ошибаются."*

**Что можно найти:**
- **Реликвии Overlord** — части его изначальной силы
- **Коронные регалии** — символы власти, дающие уникальные способности
- **Осколки трона** — материал для восстановления истинной мощи
- **Воспоминания величия** — открывают новые способности и заклинания

**Механика:** Финальные боссы (100+ HP). Уникальные награды и сюжетные элементы.

---

## 🎩 Прогрессия Оверлорда

### 👑 **Система душ и восстановления силы**

> *"Вы думали, что герои качаются? Ха. Вы просто были не готовы к системе `Овер-XP`™"*

Overlord восстанавливает свои силы, собирая **души павших врагов и героев**. Каждое убийство приносит определённое количество душ, которые становятся валютой его возрождения.

#### **💀 Источники душ:**
- **Падальщики (90-100):** 1-2 души за убийство
- **Мародёры (70-89):** 3-5 душ за убийство  
- **Наёмники (40-69):** 8-12 душ за убийство
- **Лорды войны (20-39):** 20-30 душ за убийство
- **Узурпаторы (1-19):** 50-100 душ за убийство
- **D&D-пати:** Бонус +50% душ за групповое убийство
- **Боссы:** 200-500 душ в зависимости от уровня

### ⚡ **Уровни силы Overlord**

При накоплении нужного количества душ, Overlord **повышает уровень силы**:

#### **📊 Базовые улучшения:**
- **Увеличивает здоровье** — +15 HP за уровень
- **Увеличивает урон** — +3 урона за уровень  
- **Увеличивает скорость атаки** — +5% за уровень
- **Увеличивает скорость движения** — +2% за уровень

#### **🔮 Новые способности по уровням:**
- **Уровень 5:** *Кровавая жажда* — пассивная регенерация +1 HP/сек
- **Уровень 10:** *Аура страха* — враги двигаются на 20% медленнее рядом с Overlord
- **Уровень 15:** *Отражение урона* — 25% получаемого урона отражается обратно
- **Уровень 20:** *Порча врагов* — атаки накладывают яд (2 урона/сек на 5 сек)
- **Уровень 25:** *Призыв теней* — временные союзники из убитых врагов
- **Уровень 30:** *Господство* — возможность обращать слабых врагов на свою сторону

#### **💔 Механика смерти и сброса:**
- **Прогрессия сохраняется только на текущем этаже**
- **При смерти:** теряется 50% накопленных душ и 25% уровней силы
- **При переходе на новый этаж:** сохраняется 75% прогрессии
- **Мета-прогрессия:** каждые 10 пройденных этажей дают постоянный бонус +5% к получению душ

### 🔥 **Геймплейная особенность: Раскручивание силы зла**

**Ключевая механика:** Несмотря на то, что игрок поднимается **с этажа 100 к 1**, **он сам становится всё сильнее**. Это создаёт уникальный эффект **"раскручивания силы зла"**:

- **Враги усиливаются** с каждым этажом наверх
- **Но Overlord растёт ещё быстрее** благодаря системе душ
- **Результат:** игрок чувствует нарастающую мощь, а не только увеличивающуюся сложность

#### **📈 Кривая прогрессии:**
```
Этаж 100: Overlord слаб (5 урона) vs Слабые враги (10 HP)
Этаж 75:  Overlord окреп (15 урона) vs Средние враги (25 HP)  
Этаж 50:  Overlord силён (35 урона) vs Сильные враги (50 HP)
Этаж 25:  Overlord могуч (70 урона) vs Элитные враги (80 HP)
Этаж 1:   Overlord-бог (150 урона) vs Финальный босс (200 HP)
```

### 💀 **Враги: Герои прошлого и настоящего**

#### **🗡️ Типы противников:**
- **Герои и авантюристы** из прошлого, вторгшиеся в подземелье
- **Порталы-призыватели** — некоторые враги прибывают через магические врата
- **D&D-пати группы** — классические составы: танк, лекарь, маг, разбойник
- **Одиночки-ветераны** — опытные воины с уникальными способностями
- **Боссы-командиры** — лидеры каждого яруса с особыми механиками

#### **🎭 Атмосферные детали врагов:**
- **Падальщики:** "Эй, это же тот самый Overlord! Он выглядит... жалко."
- **Мародёры:** "Слышал, за его голову дают хорошую награду!"
- **Наёмники:** "Приготовься, тварь. Мы знаем твои слабости."
- **Лорды войны:** "Ты думал, что мы забыли? Пришло время расплаты!"
- **Узурпаторы:** "Как ты посмел вернуться в МОЙ замок?!"

### ⚙️ **Гринд и мета-прогрессия**

#### **🔄 Фарм душ:**
- **Очищенные участки** — игрок может возвращаться на пройденные области
- **Респаун врагов** — через 10 минут реального времени
- **Уменьшающаяся отдача** — повторные убийства дают 50% душ
- **Бонусные события** — случайные "нашествия героев" с увеличенными наградами

#### **🏰 Восстановление замка:**
- **Яма гоблинов** — потратить 500 душ, чтобы гоблины фармили за вас
- **Алтарь силы** — постоянные улучшения за накопленные души
- **Тронный зал** — мета-апгрейды, влияющие на все прохождения
- **Портал теней** — призыв союзников за души

#### **🎯 Цель системы:**
Создать ощущение **растущей мощи и доминирования**, где игрок не просто выживает, а становится всё более смертоносным воплощением зла, пока не достигнет своего истинного величия на вершине замка.

### 🎭 **Механики иерархии (обновлено)**

#### **🎯 Адаптивная сложность:**
- **Враги становятся сильнее** с каждым поднятым уровнем  
- **НО Overlord растёт ещё быстрее** благодаря системе душ
- **Лут улучшается** в зависимости от глубины
- **Интерактивы усложняются** — простые рычаги внизу, сложные головоломки наверху
- **Баланс мощи** — игрок всегда чувствует прогресс, несмотря на усиление врагов

#### **🏗️ Архитектурная эволюция:**
- **Уровни 90-100:** Сырые пещеры, разрушенные подвалы
- **Уровни 70-89:** Временные лагеря, баррикады
- **Уровни 40-69:** Укреплённые форты, организованная оборона
- **Уровни 20-39:** Роскошные залы, тронные комнаты узурпаторов
- **Уровни 1-19:** Величественные покои, восстановленная архитектура

### 🎮 **Влияние на геймплей**

#### **🗺️ Генерация уровней:**
```csharp
// LevelGenerator.cs - добавить систему иерархии
[System.Serializable]
public class LevelTier
{
    public int minLevel, maxLevel;
    public TileSet floorTiles, wallTiles, decorTiles;
    public EnemySpawnData[] enemies;
    public LootTable lootTable;
    public float difficultyMultiplier;
}
```

#### **👹 Система врагов:**
```csharp
// EnemySpawner.cs
public class EnemySpawner : MonoBehaviour
{
    public LevelTier currentTier;
    
    void SpawnEnemiesForLevel(int level)
    {
        // Выбираем врагов в зависимости от уровня иерархии
        var tier = GetTierForLevel(level);
        foreach(var enemyData in tier.enemies)
        {
            SpawnEnemy(enemyData);
        }
    }
}
```

#### **💎 Система лута:**
```csharp
// LootSystem.cs
public class LootSystem : MonoBehaviour
{
    public void DropLoot(int currentLevel, Vector3 position)
    {
        var tier = GetTierForLevel(currentLevel);
        var loot = tier.lootTable.GetRandomLoot();
        Instantiate(loot, position, Quaternion.identity);
    }
}
```

#### **📜 Интерактивные элементы:**
- **Записки и дневники** рассказывают историю каждого уровня
- **Торговцы-призраки** предлагают товары в зависимости от иерархии
- **Статуи и алтари** дают разные бонусы на разных уровнях
- **Секретные проходы** ведут к сокровищам соответствующего уровня

### 🎪 **Атмосферные детали**

#### **🕯️ Освещение по уровням:**
- **Глубины (90-100):** Тусклые факелы, мерцающие свечи
- **Средние уровни (40-89):** Магические кристаллы, костры
- **Верхние уровни (1-39):** Роскошные люстры, божественный свет

#### **🎵 Звуковое сопровождение:**
- **Низ:** Капли воды, скрежет крыс, стоны отчаяния
- **Середина:** Лязг оружия, боевые кличи, магические заклинания
- **Верх:** Органная музыка, хоры, звуки власти

#### **🎨 Визуальный стиль:**
- **Архитектура ухудшается** с глубиной — от мраморных залов до гнилых пещер
- **Цветовая палитра** меняется — золото и пурпур наверху, серость и коричневый внизу
- **Детали декора** — богатые гобелены наверху, кости и мусор внизу

---

## 🧱 Структура уровней

Уровни создаются с помощью Unity Tilemap. Используются 3 слоя: пол (Tilemap_Floor), стены (Tilemap_Walls), декоративные элементы (Tilemap_Decor). Тайлы хранятся в TileDatabase. Генерация уровня происходит через LevelGenerator, который случайно распределяет тайлы по сетке 20x20.

### Архитектура тайловой системы

#### 🗺️ **Структура Tilemap в Unity:**
```
Grid GameObject (2D Grid)
├── Tilemap_Floor (фоновый слой)
│   ├── TilemapRenderer
│   └── [без коллайдеров]
├── Tilemap_Walls (слой стен)
│   ├── TilemapRenderer
│   ├── TilemapCollider2D
│   ├── CompositeCollider2D (Used By Composite = true)
│   ├── Rigidbody2D (Static)
│   └── Tag: "Wall"
└── Tilemap_Decor (декоративный слой)
    ├── TilemapRenderer
    └── [без коллайдеров]
```

#### 🎲 **TileDatabase.cs:**
- **Тайлы пола:** `floorTile_1`, `floorTile_2`, `floorTile_3`
- **Тайлы стен:** `wallTile_1`, `wallTile_2`
- **Декоративные тайлы:** `decorTile_1`, `decorTile_2`, `decorTile_3`
- **Методы:** `GetRandomFloorTile()`, `GetRandomWallTile()`, `GetRandomDecorTile()`

#### 🏗️ **LevelGenerator.cs:**
- **Генерация 20x20 уровней** с тремя слоями
- **Случайное размещение тайлов** из TileDatabase
- **Стены по периметру** для ограничения игровой области
- **Декор внутри уровня** с настраиваемой вероятностью (15% по умолчанию)
- **Автоматическая настройка коллизий** для стен

#### 📋 **Настройка в Unity:**
1. Создать `Grid` объект в сцене
2. Добавить 3 дочерних Tilemap: `Tilemap_Floor`, `Tilemap_Walls`, `Tilemap_Decor`
3. Создать объект с `TileDatabase.cs` и назначить тайлы
4. Добавить `LevelGenerator.cs` на объект с Grid
5. Настроить ссылки на Tilemap и TileDatabase в инспекторе
6. Запустить сцену — уровень сгенерируется автоматически

---

## 🏗️ Стартовая архитектура

Игра работает на Unity. Игрок управляется через Rigidbody2D, окружение создано на основе Tilemap, управление происходит через PlayerMovement.cs. Тайлы подгружаются через TileDatabase и размещаются скриптом LevelGenerator. Камера следует за игроком.

### 🎮 **Основные системы:**

#### **Player System:**
- **PlayerMovement.cs** — управление через WASD/стрелки
- **PlayerAttack.cs** — базовая атака мечом
- **HealthSystem.cs** — система здоровья и урона
- **Компоненты:** Rigidbody2D (gravity=0), BoxCollider2D, SpriteRenderer

#### **Camera System:**
- **CameraFollow.cs** — плавное следование за игроком
- **Настройки:** Vector3.Lerp для плавности, настраиваемое смещение
- **Функции:** мёртвая зона, ограничения по границам

#### **Level System:**
- **TileDatabase.cs** — хранилище всех тайлов (пол, стены, декор)
- **LevelGenerator.cs** — процедурная генерация уровней 20x20
- **Tilemap структура:** 3 слоя (Floor, Walls, Decor)
- **Коллизии:** автоматическая настройка для стен

#### **Enemy System:**
- **EnemyAI.cs** — ИИ с состояниями (Patrol, Chase, Attack, Return)
- **EnemyAttack.cs** — система атаки врагов
- **HealthSystem.cs** — общая система здоровья

### 🔧 **Архитектурные принципы:**

1. **Модульность** — каждая система независима
2. **Переиспользование** — HealthSystem используется для игрока и врагов
3. **Настраиваемость** — все параметры через [SerializeField]
4. **Отладка** — логгирование и визуальные гизмо
5. **Расширяемость** — публичные методы для интеграции

### 📁 **Структура проекта:**
```
Assets/
├── Scripts/           # Все C# скрипты
├── Tiles/            # Tile ресурсы Unity
├── Sprites/          # PNG спрайты
├── Prefabs/          # Префабы объектов
├── Scenes/           # Unity сцены
└── Palettes/         # Tile Palette
```

### 🎯 **Точки интеграции:**
- **Анимации:** PlayerMovement.GetMovementDirection() для аниматора
- **Звуки:** события в PlayerAttack, HealthSystem
- **UI:** события HealthSystem для отображения здоровья
- **Прогрессия:** публичные методы для изменения параметров

---

## ✅ Реализованные механики (Спринт 1-2)

### 1. Система движения игрока (`PlayerMovement.cs`)

**Статус:** ✅ Реализовано  
**Файл:** `src/PlayerMovement.cs`

#### Что реализовано:
- **2D Top-Down движение** с поддержкой WASD и стрелок
- **Rigidbody2D** с отключенной гравитацией (`gravityScale = 0`)
- **Заморозка вращения** для предотвращения поворотов при столкновениях
- **Нормализация диагонального движения** — скорость одинакова во всех направлениях
- **Система коллизий** с логгированием столкновений
- **Обработка триггеров** для будущих механик (сбор предметов, переходы)

#### Как это работает:
```csharp
// В Update() считываем ввод
float horizontalInput = Input.GetAxisRaw("Horizontal");
float verticalInput = Input.GetAxisRaw("Vertical");

// В FixedUpdate() применяем движение
Vector2 movement = movementInput.normalized * moveSpeed;
rb2D.velocity = movement;
```

#### Публичные методы для других систем:
- `GetMovementDirection()` — текущее направление движения
- `GetLastMovementDirection()` — последнее направление (для анимаций)
- `IsMoving()` — проверка движения
- `GetCurrentSpeed()` — текущая скорость
- `SetMoveSpeed(float)` — изменение скорости
- `StopMovement()` — остановка игрока

#### Как расширять:
- Добавить анимационную систему, используя `GetMovementDirection()`
- Интегрировать звуковые эффекты в `HandleWallCollision()`
- Добавить эффекты частиц при столкновениях
- Реализовать временные модификаторы скорости

---

### 2. Генерация уровней (`LevelGenerator.cs`)

**Статус:** ✅ Реализовано  
**Файл:** `src/LevelGenerator.cs`

#### Что реализовано:
- **Генерация комнат** размером 10x10 тайлов (настраивается)
- **Двухслойная система Tilemap** (пол + стены)
- **Автоматическая настройка коллизий** с `TilemapCollider2D` и `CompositeCollider2D`
- **Автоматическое назначение тегов** ("Wall" для стен)
- **Валидация компонентов** с подробным логгированием
- **Утилитарные методы** для получения позиций в комнате

#### Архитектура Tilemap:
```
Grid GameObject
├── Floor Tilemap (без коллизий)
│   └── TilemapRenderer
└── Wall Tilemap (с коллизиями)
    ├── TilemapRenderer
    ├── TilemapCollider2D
    ├── CompositeCollider2D
    └── Rigidbody2D (Static)
```

#### Настройка в Unity:
1. Создать `Grid` → `Tilemap` → `Tilemap` (два раза)
2. Назначить `Floor Tilemap` и `Wall Tilemap` в инспекторе
3. Создать `TileBase` ресурсы для пола и стен
4. Назначить тайлы в инспекторе
5. Запустить сцену — комната сгенерируется автоматически

#### Публичные методы:
- `GenerateRoom()` — генерация с текущими настройками
- `GenerateRoom(int, int)` — генерация с заданными размерами
- `GetRoomCenter()` — центр комнаты в мировых координатах
- `GetRandomPositionInRoom()` — случайная позиция внутри комнаты

#### Как расширять:
- Добавить различные типы комнат (L-образные, круглые)
- Реализовать процедурную генерацию коридоров
- Добавить размещение врагов и предметов
- Интегрировать с системой переходов между уровнями

---

### 3. Система коллизий

**Статус:** ✅ Реализовано  
**Интеграция:** `PlayerMovement.cs` + `LevelGenerator.cs`

#### Что реализовано:
- **Обработка столкновений** через `OnCollisionEnter2D`
- **Обработка триггеров** через `OnTriggerEnter2D`
- **Специальная логика для стен** с тегом "Wall"
- **Подробное логгирование** всех столкновений
- **Автоматическая настройка коллайдеров** в генераторе уровней

#### Как это работает:
```csharp
void OnCollisionEnter2D(Collision2D collision)
{
    if (collision.gameObject.CompareTag("Wall"))
    {
        HandleWallCollision(collision);
    }
}
```

#### Настройка тегов:
- **"Wall"** — стены, создаваемые `LevelGenerator`
- **"Player"** — игрок (назначить вручную)
- **"Enemy"** — враги (назначить вручную)
- Другие теги добавляются по мере необходимости

---

### 4. Боевая система (Спринт 2)

#### 4.1 Система атаки игрока (`PlayerAttack.cs`)

**Статус:** ✅ Реализовано  
**Файл:** `src/PlayerAttack.cs`

#### Что реализовано:
- **Базовая атака мечом** с левой кнопкой мыши или пробелом
- **Направленная атака** — атака в направлении движения или последнего направления
- **Система перезарядки** — предотвращает спам атак
- **Коллайдер атаки** — динамически создается и позиционируется
- **Интеграция с PlayerMovement** — получает направление атаки
- **Визуальные эффекты** — поддержка эффектов атаки
- **Анимационная поддержка** — готовность к интеграции с Animator

#### Как это работает:
```csharp
// Получаем направление атаки от PlayerMovement
Vector2 movementDirection = playerMovement.GetMovementDirection();
if (movementDirection.magnitude > 0.1f)
{
    attackDirection = movementDirection.normalized;
}

// Создаем коллайдер атаки
CircleCollider2D attackCollider = new GameObject("AttackCollider").AddComponent<CircleCollider2D>();
attackCollider.radius = attackRange;
attackCollider.isTrigger = true;
```

#### Настройка в Unity:
1. Добавить `PlayerAttack.cs` на игрока
2. Настроить параметры атаки в инспекторе
3. Добавить эффекты атаки (опционально)
4. Настроить Animator с триггером "Attack" (опционально)

#### Публичные методы:
- `IsAttacking()` — проверка состояния атаки
- `CanAttack()` — готовность к атаке
- `GetAttackDirection()` — направление атаки
- `SetAttackDamage(float)` — изменение урона
- `SetAttackRange(float)` — изменение дальности
- `SetAttackCooldown(float)` — изменение перезарядки

---

#### 4.2 Система здоровья (`HealthSystem.cs`)

**Статус:** ✅ Реализовано  
**Файл:** `src/HealthSystem.cs`

#### Что реализовано:
- **Универсальная система здоровья** для игрока и врагов
- **Получение урона** с системой неуязвимости
- **Лечение** и восстановление здоровья
- **Визуальные эффекты** — мигание при получении урона
- **События Unity** — интеграция с UI и другими системами
- **Эффекты смерти** — автоматическое уничтожение объектов
- **Отладка** — полоска здоровья в редакторе

#### Как это работает:
```csharp
// Нанесение урона
public void TakeDamage(float damage)
{
    if (isDead || isInvulnerable) return;
    
    currentHealth = Mathf.Max(0, currentHealth - damage);
    isInvulnerable = true;
    invulnerabilityTime = Time.time + invulnerabilityDuration;
    
    onHealthChanged?.Invoke(currentHealth);
    onDamageTaken?.Invoke(damage);
}
```

#### События Unity:
- `onDeath` — вызывается при смерти
- `onHealthChanged` — вызывается при изменении здоровья
- `onDamageTaken` — вызывается при получении урона
- `onHealed` — вызывается при лечении

#### Публичные методы:
- `TakeDamage(float)` — нанесение урона
- `Heal(float)` — лечение
- `RestoreFullHealth()` — полное восстановление
- `IsDead()` — проверка смерти
- `IsInvulnerable()` — проверка неуязвимости
- `GetHealthPercentage()` — процент здоровья (0-1)

---

#### 4.3 ИИ врагов (`EnemyAI.cs`)

**Статус:** ✅ Реализовано  
**Файл:** `src/EnemyAI.cs`

#### Что реализовано:
- **Состояния ИИ** — Patrol, Chase, Attack, Return
- **Патрулирование** — случайное движение в заданной зоне
- **Преследование игрока** — при обнаружении в зоне видимости
- **Атака** — при приближении к игроку
- **Возврат к начальной позиции** — при потере игрока
- **Визуальная отладка** — отображение зон в редакторе

#### Состояния ИИ:
```csharp
enum AIState { Patrol, Chase, Attack, Return }

// Логика переходов:
// Patrol → Chase (игрок в зоне обнаружения)
// Chase → Attack (игрок в зоне атаки)
// Chase → Return (игрок покинул зону обнаружения)
// Return → Patrol (враг вернулся к начальной позиции)
```

#### Настройка в Unity:
1. Добавить `EnemyAI.cs` на врага
2. Настроить параметры ИИ в инспекторе
3. Добавить `EnemyAttack.cs` для атаки
4. Добавить `HealthSystem.cs` для здоровья
5. Настроить тег "Enemy"

#### Публичные методы:
- `GetCurrentState()` — текущее состояние ИИ
- `IsChasing()` — проверка преследования
- `IsAttacking()` — проверка атаки
- `SetMoveSpeed(float)` — изменение скорости
- `SetDetectionRange(float)` — изменение зоны обнаружения
- `ForceChase()` — принудительное преследование

---

#### 4.4 Атака врагов (`EnemyAttack.cs`)

**Статус:** ✅ Реализовано  
**Файл:** `src/EnemyAttack.cs`

#### Что реализовано:
- **Атака по игроку** — автоматическая атака при приближении
- **Система перезарядки** — предотвращает спам атак
- **Коллайдер атаки** — динамическое создание и позиционирование
- **Визуальные эффекты** — поддержка эффектов атаки
- **Интеграция с EnemyAI** — вызывается из ИИ
- **Анимационная поддержка** — готовность к интеграции с Animator

#### Как это работает:
```csharp
// Выполнение атаки из EnemyAI
public void PerformAttack()
{
    if (!canAttack || isAttacking) return;
    
    // Определяем направление к игроку
    attackDirection = (player.position - transform.position).normalized;
    
    // Активируем коллайдер атаки
    attackCollider.gameObject.SetActive(true);
}
```

#### Настройка в Unity:
1. Добавить `EnemyAttack.cs` на врага
2. Настроить параметры атаки в инспекторе
3. Добавить эффекты атаки (опционально)
4. Убедиться, что у игрока есть тег "Player"

#### Публичные методы:
- `PerformAttack()` — выполнение атаки (вызывается из EnemyAI)
- `IsAttacking()` — проверка состояния атаки
- `CanAttack()` — готовность к атаке
- `SetAttackDamage(float)` — изменение урона
- `SetAttackRange(float)` — изменение дальности

---

## ⚔️ Боевые механики

Боевая система Overlord: Rise and Slice сочетает в себе динамичный экшен в стиле Devil May Cry с тактической глубиной Dark Souls. Игрок должен мастерски владеть комбинациями атак, уклонений и позиционирования, чтобы выжить в жестоких схватках с полчищами врагов.

### 🎮 Управление и перемещение

#### Базовое движение
**Описание:**  
Overlord перемещается по полю боя с плавностью и точностью, необходимой для выживания. Каждое движение должно быть осознанным — один неверный шаг может стоить жизни.

**Управление:**
- **WASD** — основное движение во всех направлениях
- **Mouse** — направление взгляда и прицеливания
- **Нормализация диагонального движения** — скорость одинакова во всех направлениях

**Техническая реализация:**
- `PlayerMovement.cs` — основная логика перемещения
- `Rigidbody2D` с отключенной гравитацией и заморозкой вращения
- `BoxCollider2D` для коллизий с окружением
- `FixedUpdate()` для стабильной физики движения

**Настройки:**
- `moveSpeed` (float): базовая скорость движения
- `acceleration` (float): ускорение при начале движения
- `deceleration` (float): замедление при остановке
- `movementSmoothing` (float): сглаживание движения

---

#### Система уклонений (Dash)
**Описание:**  
Быстрый рывок с кратковременными кадрами неуязвимости позволяет избегать атак врагов и быстро менять позицию. Мастерское использование дэша — ключ к выживанию.

**Управление:**
- **Shift** или **Space** — выполнение дэша в направлении движения
- **Cooldown система** — предотвращает спам-использование
- **I-frames** — 0.2-0.3 секунды неуязвимости

**Техническая реализация:**
- `PlayerDash.cs` — логика рывков и неуязвимости
- `Coroutine` для управления длительностью и перезарядкой
- `OnTriggerEnter2D` игнорируется во время i-frames
- `TrailRenderer` для визуального эффекта следа

**Скрипты:**
- `PlayerDash.cs` — основная логика дэша
- `InvulnerabilityFrames.cs` — система неуязвимости
- `DashCooldown.cs` — управление перезарядкой

**Настройки:**
- `dashDistance` (float): дальность рывка
- `dashDuration` (float): время выполнения дэша
- `invulnerabilityTime` (float): длительность неуязвимости
- `dashCooldown` (float): время перезарядки
- `dashSpeed` (float): скорость во время рывка

---

### ⚔️ Типы атак

#### Ближняя атака (Melee Combat)
**Описание:**  
Основа боевой системы — мощные удары мечом, которые можно комбинировать в разрушительные серии. Каждый удар имеет свой тайминг, радиус поражения и эффект отталкивания.

**Управление:**
- **Mouse0** или **X** — базовая атака
- **Combo-chain система** — до 3 ударов в серии
- **Timing-based** — правильный тайминг увеличивает урон

**Механики:**
- **Первый удар** — быстрый, средний урон, малое отталкивание
- **Второй удар** — средняя скорость, повышенный урон
- **Третий удар** — медленный, максимальный урон, сильное отталкивание
- **Combo reset** — пауза сбрасывает цепочку

**Техническая реализация:**
- `MeleeAttack.cs` — основная логика ближнего боя
- `ComboSystem.cs` — управление цепочками ударов
- `AttackCollider` — динамический коллайдер для каждого удара
- `Animator` — анимации атак с событиями

**Скрипты:**
- `MeleeAttack.cs` — базовая система ближнего боя
- `ComboSystem.cs` — цепочки и тайминг ударов
- `WeaponController.cs` — управление оружием и эффектами
- `HitboxManager.cs` — динамические коллайдеры атак

**Настройки:**
- `attackDamage` (float[]): урон для каждого удара в комбо
- `attackRange` (float[]): радиус каждого удара
- `attackSpeed` (float[]): скорость анимации ударов
- `comboWindow` (float): окно для продолжения комбо
- `knockbackForce` (float[]): сила отталкивания для каждого удара

---

#### Дальняя атака (Ranged Combat)
**Описание:**  
Магические снаряды из тёмной энергии позволяют атаковать врагов на расстоянии. Каждый снаряд обладает уникальными свойствами и траекторией полёта.

**Управление:**
- **Mouse1** или **Y** — выпуск снаряда
- **Направление мыши** — траектория полёта
- **Ограниченные заряды** — система ресурсов

**Типы снарядов:**
- **Огненный шар** — прямолинейный, средний урон, поджигает
- **Череп** — медленный, высокий урон, пронзает врагов
- **Пепельная волна** — широкий конус, малый урон, замедляет

**Техническая реализация:**
- `RangedAttack.cs` — система дальних атак
- `Projectile.cs` — поведение снарядов
- `ProjectilePool.cs` — оптимизация через object pooling
- `TrajectoryPreview.cs` — предпросмотр траектории

**Скрипты:**
- `RangedAttack.cs` — основная логика дальних атак
- `Projectile.cs` — базовый класс для всех снарядов
- `FireballProjectile.cs` — огненные снаряды
- `SkullProjectile.cs` — снаряды-черепа
- `AshWaveProjectile.cs` — пепельные волны

**Настройки:**
- `projectileSpeed` (float): скорость полёта снаряда
- `projectileDamage` (float): урон снаряда
- `projectileRange` (float): максимальная дальность
- `ammoCount` (int): количество зарядов
- `reloadTime` (float): время перезарядки
- `projectileEffects` (ParticleSystem[]): визуальные эффекты

---

### 🧠 Система ИИ врагов

#### Архитектура ИИ
**Описание:**  
Враги используют продвинутую систему состояний (FSM), которая делает их поведение непредсказуемым и опасным. Каждый тип врага имеет уникальные паттерны атак и поведения.

**Состояния ИИ:**
- **Idle** — покой, поиск игрока
- **Patrol** — патрулирование территории
- **Chase** — преследование игрока
- **Attack** — выполнение атаки
- **Stunned** — оглушение после получения урона
- **Death** — смерть и исчезновение

**Техническая реализация:**
- `EnemyAI.cs` — базовый ИИ с FSM
- `EnemyVision.cs` — система обнаружения игрока
- `EnemyNavigation.cs` — навигация и pathfinding
- `StateMachine.cs` — универсальная машина состояний

**Скрипты:**
- `EnemyAI.cs` — основная логика ИИ
- `EnemyStateMachine.cs` — управление состояниями
- `EnemyVision.cs` — обнаружение и видимость
- `EnemyFormation.cs` — групповое поведение

---

#### Типы врагов

##### Медленные враги (Heavy)
**Описание:**  
Массивные противники с высоким здоровьем, но медленными атаками. Их удары наносят огромный урон, но имеют долгую анимацию, которую можно прервать.

**Поведение:**
- Медленное приближение к игроку
- Мощные атаки с долгой подготовкой
- Высокая устойчивость к отталкиванию
- Могут ломать препятствия

**Скрипты:**
- `HeavyEnemyAI.cs` — специфичная логика
- `HeavyAttack.cs` — мощные атаки
- `DestructibleObstacle.cs` — разрушение препятствий

---

##### Быстрые враги (Agile)
**Описание:**  
Подвижные противники, которые быстро перемещаются и атакуют короткими сериями. Слабы в защите, но опасны в группах.

**Поведение:**
- Быстрые рывки к игроку
- Серии быстрых атак
- Уклонение от медленных атак игрока
- Атаки в группах

**Скрипты:**
- `AgileEnemyAI.cs` — логика быстрых врагов
- `EnemyDash.cs` — рывки врагов
- `SwarmBehavior.cs` — групповые атаки

---

##### Дальнобойные враги (Ranged)
**Описание:**  
Враги, атакующие на расстоянии магическими снарядами или ядовитыми плевками. Стараются держать дистанцию и атаковать из укрытий.

**Поведение:**
- Поддержание дистанции с игроком
- Атаки снарядами через препятствия
- Отступление при приближении игрока
- Поиск выгодных позиций

**Скрипты:**
- `RangedEnemyAI.cs` — логика дальнобойных врагов
- `EnemyProjectile.cs` — снаряды врагов
- `CoverSystem.cs` — использование укрытий

---

### 🩸 Урон и эффекты попадания

#### Система нанесения урона
**Описание:**  
Каждое попадание сопровождается комплексом визуальных и звуковых эффектов, создающих ощущение веса и импакта. Урон рассчитывается с учётом типа атаки, брони врага и критических попаданий.

**Механики урона:**
- **Базовый урон** — основное значение атаки
- **Критические попадания** — 2x урон при удачном тайминге
- **Система брони** — снижение входящего урона
- **Элементальные эффекты** — огонь, лёд, яд

**Техническая реализация:**
- `DamageSystem.cs` — расчёт и применение урона
- `CriticalHit.cs` — система критических попаданий
- `ArmorSystem.cs` — защита и сопротивления
- `StatusEffects.cs` — временные эффекты

**Скрипты:**
- `DamageSystem.cs` — основная система урона
- `HealthSystem.cs` — управление здоровьем
- `CriticalHitDetector.cs` — определение критических попаданий
- `DamageCalculator.cs` — расчёт финального урона

---

#### Визуальные эффекты попадания
**Описание:**  
Каждое попадание должно ощущаться игроком через мощную визуальную и тактильную обратную связь. Эффекты варьируются в зависимости от силы и типа атаки.

**Эффекты:**
- **Hit-Stop** — заморозка на 0.1-0.2 секунды
- **Screen Shake** — тряска камеры
- **Blood Splatter** — брызги крови
- **Damage Numbers** — всплывающие числа урона
- **Flash Effect** — мигание спрайта врага

**Техническая реализация:**
- `HitStopManager.cs` — заморозка времени
- `CameraShake.cs` — эффекты тряски камеры
- `ParticleSystem` — кровь и искры
- `DamagePopup.cs` — всплывающий текст
- `FlashEffect.cs` — мигание при уроне

**Скрипты:**
- `HitEffectManager.cs` — координация всех эффектов
- `CameraShake.cs` — тряска экрана
- `BloodSplatter.cs` — эффекты крови
- `DamagePopup.cs` — числа урона
- `HitStopController.cs` — заморозка времени

**Настройки:**
- `hitStopDuration` (float): длительность заморозки
- `shakeIntensity` (float): сила тряски камеры
- `bloodParticleCount` (int): количество частиц крови
- `damagePopupSize` (float): размер текста урона
- `flashDuration` (float): длительность мигания

---

### 📉 UI и обратная связь

#### Интерфейс боя
**Описание:**  
Минималистичный UI, который предоставляет всю необходимую информацию, не загромождая экран. Все элементы интегрированы в игровой мир и следуют готической эстетике.

**Элементы UI:**
- **Полоска здоровья игрока** — в углу экрана
- **Индикатор дэша** — cooldown и готовность
- **Счётчик душ** — текущее количество ресурсов
- **Полоски здоровья врагов** — над головами
- **Combo counter** — текущая серия ударов

**Техническая реализация:**
- `UIManager.cs` — управление всем интерфейсом
- `HealthBar.cs` — полоски здоровья
- `CooldownIndicator.cs` — индикаторы перезарядки
- `ComboDisplay.cs` — отображение комбо
- `SoulCounter.cs` — счётчик ресурсов

**Скрипты:**
- `CombatUI.cs` — основной интерфейс боя
- `PlayerHealthUI.cs` — здоровье игрока
- `EnemyHealthBar.cs` — здоровье врагов
- `AbilityCooldowns.cs` — перезарядки способностей
- `FloatingText.cs` — всплывающий текст

---

#### Система анимаций
**Описание:**  
Плавные и выразительные анимации, которые ясно передают состояние персонажа и его действия. Каждая анимация имеет чёткие кадры атаки и восстановления.

**Типы анимаций:**
- **Idle** — состояние покоя с дыханием
- **Movement** — ходьба в 8 направлениях
- **Attack Combo** — серия из 3 ударов
- **Dash** — рывок с размытием
- **Hit Reaction** — реакция на получение урона
- **Death** — смерть с разложением

**Техническая реализация:**
- `Animator Controller` — управление состояниями
- `Animation Events` — события в ключевых кадрах
- `Blend Trees` — плавные переходы
- `Override Controllers` — вариации для врагов

**Скрипты:**
- `AnimationController.cs` — управление анимациями
- `AttackAnimationEvents.cs` — события атак
- `MovementAnimator.cs` — анимации движения
- `DeathAnimator.cs` — анимации смерти

---

### 🌪 Расширенные механики

#### Система душ
**Описание:**  
Души павших врагов служат основной валютой для развития персонажа. Они притягиваются к игроку и могут быть потеряны при смерти, добавляя риск в сражения.

**Механики:**
- **Автосбор** — души притягиваются к игроку
- **Множители** — комбо увеличивает количество душ
- **Потеря при смерти** — можно вернуться и забрать
- **Трата на улучшения** — постоянное развитие

**Техническая реализация:**
- `SoulManager.cs` — управление системой душ
- `SoulOrb.cs` — отдельные души с притяжением
- `SoulMultiplier.cs` — бонусы за комбо
- `SoulUpgrade.cs` — трата на улучшения

**Скрипты:**
- `SoulSystem.cs` — основная система душ
- `SoulCollector.cs` — сбор и притяжение
- `SoulBank.cs` — хранение и трата
- `SoulMultiplier.cs` — множители и бонусы

---

#### Система парирования
**Описание:**  
Продвинутая механика, позволяющая опытным игрокам отражать атаки врагов в точный момент, получая преимущество в бою и возможность контратаки.

**Механики:**
- **Timing Window** — окно в 0.2-0.3 секунды
- **Perfect Parry** — точное парирование оглушает врага
- **Parry Counter** — автоматическая контратака
- **Stamina Cost** — тратит выносливость

**Техническая реализация:**
- `ParrySystem.cs` — основная логика парирования
- `TimingWindow.cs` — окно для успешного парирования
- `CounterAttack.cs` — автоматические контратаки
- `ParryEffects.cs` — визуальные эффекты

**Скрипты:**
- `ParryController.cs` — управление парированием
- `ParryTiming.cs` — расчёт тайминга
- `ParryCounter.cs` — контратаки
- `ParryFeedback.cs` — обратная связь

---

#### Система ярости (Bloodrage)
**Описание:**  
Накопление ярости через успешные атаки активирует режим берсерка, значительно увеличивающий урон и скорость, но делающий игрока более уязвимым.

**Механики:**
- **Накопление** — каждое попадание добавляет ярость
- **Активация** — при 100% включается режим
- **Бонусы** — +50% урон, +30% скорость
- **Уязвимость** — +25% получаемый урон

**Техническая реализация:**
- `BloodrageSystem.cs` — система накопления ярости
- `RageMode.cs` — активный режим берсерка
- `RageEffects.cs` — визуальные эффекты
- `RageDecay.cs` — постепенное снижение

**Скрипты:**
- `BloodrageController.cs` — основная логика ярости
- `RageMeter.cs` — накопление и отображение
- `BerserkMode.cs` — активный режим
- `RageVisuals.cs` — визуальные эффекты

---

#### Комбо-система (Style Rank)
**Описание:**  
Система оценки стиля боя в духе Devil May Cry, поощряющая разнообразие атак, тайминг и избежание урона. Высокий ранг даёт бонусы к получаемым душам.

**Ранги стиля:**
- **D** — базовый уровень
- **C** — хорошие комбинации
- **B** — отличная техника
- **A** — мастерский бой
- **S** — легендарный стиль

**Факторы оценки:**
- **Разнообразие атак** — использование разных типов
- **Длина комбо** — количество попаданий подряд
- **Избежание урона** — время без получения урона
- **Точность** — процент попаданий

**Техническая реализация:**
- `StyleSystem.cs` — расчёт ранга стиля
- `ComboTracker.cs` — отслеживание комбинаций
- `StyleMultiplier.cs` — бонусы за высокий ранг
- `StyleDisplay.cs` — отображение текущего ранга

**Скрипты:**
- `StyleRankSystem.cs` — основная система рангов
- `ComboAnalyzer.cs` — анализ комбинаций
- `StyleCalculator.cs` — расчёт итогового ранга
- `StyleRewards.cs` — награды за высокий стиль

---

### 🔁 Архитектура кода

#### Структура боевой системы
**Описание:**  
Модульная архитектура позволяет легко расширять и модифицировать боевую систему. Каждый компонент отвечает за свою область и взаимодействует с другими через чётко определённые интерфейсы.

**Основные компоненты:**
- **PlayerController** — агрегирует ввод и состояние игрока
- **CombatManager** — координирует все боевые системы
- **DamageSystem** — обработка урона и эффектов
- **EffectManager** — визуальные и звуковые эффекты

**Принципы SOLID:**
- **Single Responsibility** — каждый класс имеет одну задачу
- **Open/Closed** — открыт для расширения, закрыт для изменения
- **Liskov Substitution** — наследники взаимозаменяемы
- **Interface Segregation** — специфичные интерфейсы
- **Dependency Inversion** — зависимость от абстракций

---

#### Система событий
**Описание:**  
Unity Events и custom event system обеспечивают слабую связанность между компонентами и позволяют легко добавлять новые реакции на игровые события.

**Основные события:**
- **OnPlayerAttack** — игрок атакует
- **OnEnemyHit** — враг получает урон
- **OnEnemyDeath** — враг умирает
- **OnPlayerDash** — игрок делает рывок
- **OnComboBreak** — комбо прерывается

**Техническая реализация:**
- `UnityEvent` — встроенная система событий
- `EventManager.cs` — централизованный менеджер событий
- `Observer Pattern` — подписка на события
- `Action<T>` — типизированные события

---

#### Структура файлов
**Описание:**  
Организованная файловая структура облегчает навигацию по проекту и поддержку кода. Все боевые скрипты логически сгруппированы по функциональности.

**Структура папок:**
```
Assets/Scripts/Combat/
├── Player/
│   ├── PlayerController.cs
│   ├── PlayerMovement.cs
│   ├── PlayerDash.cs
│   ├── MeleeAttack.cs
│   └── RangedAttack.cs
├── Enemies/
│   ├── EnemyAI.cs
│   ├── EnemyAttack.cs
│   ├── HeavyEnemyAI.cs
│   ├── AgileEnemyAI.cs
│   └── RangedEnemyAI.cs
├── Systems/
│   ├── DamageSystem.cs
│   ├── HealthSystem.cs
│   ├── SoulSystem.cs
│   ├── ParrySystem.cs
│   └── StyleSystem.cs
├── Effects/
│   ├── HitEffectManager.cs
│   ├── CameraShake.cs
│   ├── BloodSplatter.cs
│   └── DamagePopup.cs
├── UI/
│   ├── CombatUI.cs
│   ├── HealthBar.cs
│   ├── StyleDisplay.cs
│   └── ComboCounter.cs
└── Interfaces/
    ├── IDamageable.cs
    ├── IAttacker.cs
    ├── IStylish.cs
    └── IUpgradeable.cs
```

---

#### Теги и слои
**Описание:**  
Правильная настройка тегов и слоёв критически важна для корректной работы боевой системы и оптимизации производительности.

**Необходимые теги:**
- **"Player"** — игрок
- **"Enemy"** — враги
- **"Wall"** — стены и препятствия
- **"Projectile"** — снаряды
- **"Soul"** — души для сбора

**Слои (Layers):**
- **Player** (слой 8) — игрок и его атаки
- **Enemy** (слой 9) — враги и их атаки
- **Environment** (слой 10) — окружение
- **Projectiles** (слой 11) — все снаряды
- **UI** (слой 5) — интерфейс

**Настройка коллизий:**
- Player не сталкивается с Projectiles игрока
- Enemy не сталкивается с Projectiles врагов
- Projectiles исчезают при столкновении с Environment

---

### 🎯 Этапы реализации

#### Этап 1: Базовый бой (MVP)
**Цель:** Создать минимально работающую боевую систему

**Компоненты:**
- [x] `PlayerMovement.cs` — базовое движение
- [x] `PlayerAttack.cs` — простая атака
- [x] `EnemyAI.cs` — базовый ИИ
- [x] `HealthSystem.cs` — система здоровья
- [ ] `DamageSystem.cs` — нанесение урона

**Критерии готовности:**
- Игрок может двигаться и атаковать
- Враги реагируют на игрока и атакуют
- Урон корректно применяется
- Враги умирают при нулевом здоровье

---

#### Этап 2: Расширенный бой
**Цель:** Добавить глубину и вариативность

**Компоненты:**
- [ ] `PlayerDash.cs` — система рывков
- [ ] `ComboSystem.cs` — комбинации атак
- [ ] `RangedAttack.cs` — дальние атаки
- [ ] `HitEffectManager.cs` — эффекты попадания
- [ ] `SoulSystem.cs` — система душ

**Критерии готовности:**
- Дэш работает с неуязвимостью
- Комбо увеличивают урон
- Дальние атаки функциональны
- Эффекты попадания впечатляют
- Души собираются и тратятся

---

#### Этап 3: Продвинутые механики
**Цель:** Добавить мастерство и глубину

**Компоненты:**
- [ ] `ParrySystem.cs` — парирование
- [ ] `StyleSystem.cs` — система стиля
- [ ] `BloodrageSystem.cs` — режим ярости
- [ ] `AdvancedEnemyAI.cs` — умные враги
- [ ] `BossAI.cs` — боссы

**Критерии готовности:**
- Парирование требует навыка
- Система стиля мотивирует
- Ярость добавляет риск/награду
- Враги представляют вызов
- Боссы эпичны и сложны

---

## 🌙 Небоевые взаимодействия

Мир Overlord: Rise and Slice наполнен интерактивными элементами, которые создают атмосферу, рассказывают историю и предоставляют игроку возможности для исследования. Каждый элемент служит не только декорацией, но и механикой, углубляющей игровой опыт.

### 1. 🗿 Интерактивные объекты (статуи, руны, гробницы)

**Атмосферное описание:**  
Древние статуи некогда могущественных лордов мерцают тусклым светом, когда Overlord приближается к ним. Рунические камни хранят память о забытых заклинаниях, а потревоженные гробницы шепчут проклятия на мёртвых языках.

**Что это делает с игроком:**
- Активация даёт временные баффы (увеличение урона, скорости, здоровья)
- Открывает секретные проходы или раскрывает скрытые области
- Восстанавливает здоровье или ману за счёт душ
- Может проклинать игрока, требуя выполнения условий

**Техническая реализация:**
- BoxCollider2D с isTrigger = true для обнаружения игрока
- OnTriggerEnter2D для активации взаимодействия
- SpriteRenderer с анимацией свечения/мерцания
- AudioSource для звуковых эффектов активации
- ParticleSystem для визуальных эффектов

**Скрипты:**
- `InteractableObject.cs` — базовый класс для всех интерактивных объектов
- `StatueInteraction.cs` — специфичная логика для статуй
- `RuneStone.cs` — активация рунических камней
- `TombInteraction.cs` — взаимодействие с гробницами

**Настройки:**
- `interactionType` (enum): Buff, Heal, Curse, SecretDoor
- `effectDuration` (float): длительность эффекта в секундах
- `activationCost` (int): стоимость в душах для активации
- `oneTimeUse` (bool): можно ли использовать повторно
- `glowIntensity` (float): интенсивность свечения при приближении

---

### 2. 💎 Предметы и лут (души, амулеты, ключи)

**Атмосферное описание:**  
Души павших врагов кружатся в воздухе как светящиеся осколки, притягиваясь к Overlord. Древние амулеты излучают магическую ауру, а ржавые ключи открывают двери в забытые части подземелий.

**Что это делает с игроком:**
- Души служат валютой для улучшений и покупок
- Амулеты дают постоянные пассивные бонусы
- Ключи открывают заблокированные области
- Редкие артефакты разблокируют новые способности

**Техническая реализация:**
- CircleCollider2D с isTrigger = true для автосбора
- OnTriggerEnter2D с проверкой тега "Player"
- Rigidbody2D для физики притяжения к игроку
- Animator для анимации вращения и пульсации
- UI интеграция для отображения в инвентаре

**Скрипты:**
- `Collectible.cs` — базовый класс для всех собираемых предметов
- `SoulOrb.cs` — логика душ с магнитным притяжением
- `AmuletItem.cs` — амулеты с пассивными эффектами
- `KeyItem.cs` — ключи для дверей и сундуков
- `MagnetCollector.cs` — система притяжения предметов

**Настройки:**
- `itemType` (enum): Soul, Amulet, Key, Artifact
- `value` (int): количество душ или уровень предмета
- `magnetRange` (float): радиус притяжения к игроку
- `bobSpeed` (float): скорость левитации предмета
- `collectSound` (AudioClip): звук при сборе

---

### 3. 🧩 Загадки и плитки

**Атмосферное описание:**  
Каменные плиты с древними символами требуют активации в правильной последовательности. Загадочные механизмы скрипят при взаимодействии, открывая путь тем, кто разгадает их тайны.

**Что это делает с игроком:**
- Требует решения головоломок для прохождения
- Открывает секретные комнаты с ценными наградами
- Активирует мосты, лифты или движущиеся платформы
- Может изменить геометрию уровня

**Техническая реализация:**
- Массив GameObject для плиток-переключателей
- State Machine для отслеживания последовательности
- OnTriggerEnter2D для активации каждой плитки
- Animator для визуальной обратной связи
- Coroutine для таймеров и задержек

**Скрипты:**
- `PuzzleManager.cs` — контроллер всей головоломки
- `PuzzleTile.cs` — отдельная плитка-переключатель
- `SequencePuzzle.cs` — загадки с правильной последовательностью
- `PressurePlate.cs` — плиты, требующие постоянного давления
- `MovingPlatform.cs` — платформы, активируемые загадками

**Настройки:**
- `correctSequence` (int[]): правильная последовательность активации
- `resetOnMistake` (bool): сбрасывать ли прогресс при ошибке
- `timeLimit` (float): ограничение по времени (0 = без лимита)
- `rewardItems` (GameObject[]): награды за решение
- `hintGlowDuration` (float): длительность подсказки

---

### 4. ⚡ Ловушки (шипы, обвалы)

**Атмосферное описание:**  
Острые шипы выстреливают из стен с металлическим лязгом, а потолки угрожающе трещат перед обвалом. Древние механизмы защиты замка всё ещё функционируют, безжалостно атакуя незваных гостей.

**Что это делает с игроком:**
- Наносит урон при неосторожном движении
- Создаёт препятствия, требующие тайминга для прохождения
- Может мгновенно убить при попадании в смертельные ловушки
- Заставляет изучать паттерны и планировать маршрут

**Техническая реализация:**
- BoxCollider2D с isTrigger = true для зоны активации
- Coroutine для управления циклами активации
- OnTriggerEnter2D для нанесения урона игроку
- Animator для анимации шипов/падающих камней
- HealthSystem интеграция для урона

**Скрипты:**
- `TrapBase.cs` — базовый класс для всех ловушек
- `SpikeTrap.cs` — выдвижные шипы с циклической активацией
- `FallingRocks.cs` — обвалы с предупреждающими знаками
- `PressureTrap.cs` — ловушки, активируемые весом
- `TrapDetector.cs` — система обнаружения ловушек

**Настройки:**
- `damage` (float): урон, наносимый ловушкой
- `activationDelay` (float): задержка перед активацией
- `activeTime` (float): время активного состояния
- `cooldownTime` (float): время перезарядки
- `warningEffect` (ParticleSystem): предупреждающий эффект

---

### 5. 👻 Призрачные эхо-сцены (повторы, фантомы)

**Атмосферное описание:**  
Призрачные силуэты повторяют действия давно умерших обитателей замка. Эхо прошлого проигрывается в полупрозрачных образах, показывая фрагменты трагической истории.

**Что это делает с игроком:**
- Рассказывает историю через визуальные сцены
- Даёт подсказки о скрытых проходах или сокровищах
- Создаёт атмосферу и погружение в лор
- Может предупреждать об опасностях впереди

**Техническая реализация:**
- SpriteRenderer с полупрозрачным материалом
- Animator с записанными анимациями движения
- Timeline для сложных сцен с несколькими персонажами
- OnTriggerEnter2D для активации воспроизведения
- AudioSource для призрачных голосов и звуков

**Скрипты:**
- `GhostEcho.cs` — контроллер призрачных повторов
- `EchoTrigger.cs` — триггер для запуска эхо-сцен
- `PhantomCharacter.cs` — отдельный призрачный персонаж
- `EchoSequence.cs` — управление последовательностью сцен
- `LoreNarrator.cs` — текстовые подсказки и описания

**Настройки:**
- `echoClips` (AnimationClip[]): анимации для воспроизведения
- `ghostOpacity` (float): прозрачность призрачных фигур
- `playOnce` (bool): воспроизводить только один раз
- `narrativeText` (string[]): текст для отображения
- `audioClips` (AudioClip[]): звуковое сопровождение

---

### 6. 🕯️ Свет и тьма (свечи, огонь, реагирующий свет)

**Атмосферное описание:**  
Мерцающие свечи разгоняют тьму небольшими кругами света, а факелы на стенах вспыхивают при приближении Overlord. Тьма скрывает опасности, но свет может привлечь нежелательное внимание.

**Что это делает с игроком:**
- Ограничивает видимость, создавая напряжение
- Свет раскрывает скрытые детали и секреты
- Некоторые враги боятся света, другие — привлекаются им
- Управление освещением становится тактическим элементом

**Техническая реализация:**
- Light2D для создания источников света
- LayerMask для управления освещением объектов
- OnTriggerEnter2D для автоматического зажигания
- Coroutine для эффектов мерцания
- Shader для создания теней и световых эффектов

**Скрипты:**
- `LightSource.cs` — управление источниками света
- `CandleFlicker.cs` — эффект мерцания свечей
- `AutoTorch.cs` — факелы, зажигающиеся при приближении
- `ShadowCaster.cs` — создание динамических теней
- `LightSensitiveEnemy.cs` — враги, реагирующие на свет

**Настройки:**
- `lightIntensity` (float): яркость источника света
- `lightRadius` (float): радиус освещения
- `flickerSpeed` (float): скорость мерцания
- `autoIgniteRange` (float): расстояние автозажигания
- `burnDuration` (float): время горения (0 = бесконечно)

---

### 7. 📜 Лор и письма (заметки, дневники)

**Атмосферное описание:**  
Пожелтевшие страницы дневников рассказывают о последних днях обитателей замка. Записки, нацарапанные дрожащей рукой, раскрывают тайны проклятий и предательств.

**Что это делает с игроком:**
- Углубляет понимание истории и мотивации персонажей
- Предоставляет подсказки для головоломок и секретов
- Создаёт эмоциональную связь с миром игры
- Может давать практические советы по геймплею

**Техническая реализация:**
- OnTriggerEnter2D для обнаружения игрока
- UI Canvas для отображения текста письма
- TextMeshPro для красивого отображения текста
- ScrollRect для длинных текстов
- Input handling для закрытия письма

**Скрипты:**
- `LoreDocument.cs` — базовый класс для всех документов
- `DiaryEntry.cs` — записи в дневниках
- `Letter.cs` — письма и записки
- `LoreUI.cs` — UI для отображения текста
- `DocumentCollection.cs` — коллекция найденных документов

**Настройки:**
- `documentTitle` (string): заголовок документа
- `documentText` (string): основной текст
- `authorName` (string): автор документа
- `readOnce` (bool): можно ли читать повторно
- `unlockHint` (string): подсказка, которую даёт документ

---

### 8. 🌀 Проклятия, таймеры, зоны давления

**Атмосферное описание:**  
Проклятые зоны пульсируют тёмной энергией, медленно высасывая жизненные силы. Древняя магия создаёт области, где время течёт по-другому, а давление реальности искажает пространство.

**Что это делает с игроком:**
- Создаёт временное давление и срочность
- Постепенно наносит урон или накладывает дебаффы
- Изменяет скорость движения или время перезарядки
- Требует быстрого прохождения или поиска противоядия

**Техническая реализация:**
- BoxCollider2D с isTrigger = true для зоны эффекта
- OnTriggerStay2D для постоянного воздействия
- Coroutine для управления таймерами
- ParticleSystem для визуального обозначения зоны
- UI элементы для отображения эффектов

**Скрипты:**
- `CursedZone.cs` — области с проклятиями
- `TimeDistortion.cs` — зоны изменения времени
- `PressureField.cs` — области давления
- `CurseEffect.cs` — управление эффектами проклятий
- `ZoneDetector.cs` — обнаружение входа/выхода из зон

**Настройки:**
- `curseType` (enum): Damage, Slow, Confusion, Fear
- `effectStrength` (float): сила воздействия
- `tickRate` (float): частота применения эффекта
- `maxDuration` (float): максимальное время воздействия
- `visualEffect` (ParticleSystem): визуальное обозначение

---

### 9. 🏛️ Декоративные сцены (места силы, алтари, комнаты)

**Атмосферное описание:**  
Величественные алтари излучают ауру древней силы, а тронные залы хранят отголоски былого величия. Места силы концентрируют магическую энергию, создавая области особого значения.

**Что это делает с игроком:**
- Служит точками сохранения и восстановления
- Предоставляет доступ к улучшениям и способностям
- Создаёт ощущение прогресса и достижения
- Может телепортировать игрока между локациями

**Техническая реализация:**
- OnTriggerEnter2D для активации взаимодействия
- UI меню для выбора действий (сохранение, улучшения)
- ParticleSystem для магических эффектов
- AudioSource для атмосферных звуков
- Light2D для создания особого освещения

**Скрипты:**
- `PowerAltar.cs` — алтари силы для улучшений
- `SaveShrine.cs` — места сохранения игры
- `ThroneRoom.cs` — особые тронные залы
- `MagicCircle.cs` — телепортационные круги
- `SacredGrounds.cs` — священные места

**Настройки:**
- `altarType` (enum): Save, Upgrade, Teleport, Lore
- `requiredSouls` (int): стоимость использования
- `cooldownTime` (float): время перезарядки
- `ambientEffect` (ParticleSystem): постоянный эффект
- `activationSound` (AudioClip): звук активации

---

### 10. 🚪 Переходы между уровнями (двери, порталы, лестницы)

**Атмосферное описание:**  
Массивные двери скрипят на ржавых петлях, открывая путь в неизведанные части замка. Магические порталы мерцают нестабильной энергией, а каменные лестницы ведут в глубины подземелий.

**Что это делает с игроком:**
- Обеспечивает переходы между различными уровнями
- Создаёт ощущение исследования и открытий
- Может требовать ключи или выполнение условий
- Служит точками невозврата в сюжете

**Техническая реализация:**
- OnTriggerEnter2D для обнаружения игрока
- SceneManager.LoadScene() для смены уровней
- UI подтверждения перед переходом
- Fade эффекты для плавных переходов
- Система сохранения позиции игрока

**Скрипты:**
- `LevelTransition.cs` — базовый класс для переходов
- `Door.cs` — двери с требованиями ключей
- `MagicPortal.cs` — магические порталы
- `Staircase.cs` — лестницы между этажами
- `SceneLoader.cs` — управление загрузкой сцен

**Настройки:**
- `targetScene` (string): название целевой сцены
- `spawnPointID` (string): ID точки появления
- `requiredKey` (KeyItem): необходимый ключ
- `transitionTime` (float): время перехода
- `confirmationPrompt` (bool): показывать ли подтверждение

---

## 🎯 Интеграция небоевых элементов

### Общие принципы:
- **Атмосферность** — каждый элемент усиливает готическую атмосферу
- **Функциональность** — красота служит игровой механике
- **Вариативность** — один тип объекта может иметь разные эффекты
- **Прогрессия** — сложность взаимодействий растёт с развитием игрока

### Технические требования:
- Все интерактивные объекты наследуются от `InteractableBase.cs`
- Единая система UI для всех взаимодействий
- Сохранение состояния объектов между сессиями
- Система подсказок для новых игроков

### Балансировка:
- Награды соответствуют сложности взаимодействия
- Опасные элементы имеют визуальные предупреждения
- Альтернативные пути для разных стилей игры
- Постепенное введение новых механик

---

## 🔧 Техническая архитектура

### Структура проекта:
```
src/                    # Исходный код
├── PlayerMovement.cs   # Система движения игрока
└── LevelGenerator.cs   # Генерация уровней

assets/                 # Ресурсы игры
├── art/               # Спрайты, тайлы, текстуры
├── audio/             # Звуки и музыка
└── levels/            # Данные уровней

docs/                  # Документация
└── design.md          # Этот файл

tests/                 # Юнит-тесты (будущее)
```

### Принципы кода:
- **Модульность** — каждая система независима
- **Расширяемость** — публичные методы для интеграции
- **Отладка** — подробное логгирование с возможностью отключения
- **Документация** — все методы документированы на русском языке

---

## 🚀 Планы на следующие спринты

### Спринт 2: Боевая система ✅ ЗАВЕРШЕН
- [x] Базовая атака мечом (`PlayerAttack.cs`)
- [x] Система здоровья (`HealthSystem.cs`)
- [x] Простые враги с ИИ (`EnemyAI.cs`)
- [x] Урон и смерть (`EnemyAttack.cs`)

### Спринт 3: Прогрессия и улучшения
- [ ] Система опыта и уровней
- [ ] Сбор душ врагов
- [ ] Базовые улучшения персонажа
- [ ] Сохранение прогресса
- [ ] UI для здоровья и опыта

### Спринт 4: Процедурная генерация
- [ ] Случайные комнаты разных типов
- [ ] Коридоры между комнатами
- [ ] Размещение врагов и предметов
- [ ] Мини-карта
- [ ] Система переходов между уровнями

### Спринт 5: Продвинутые механики
- [ ] Различные типы оружия
- [ ] Способности и заклинания
- [ ] Система брони и сопротивлений
- [ ] Эффекты состояния (отравление, оглушение)
- [ ] Боссы и мини-боссы

---

## 📝 Заметки по разработке

### Использованные Unity компоненты:
- **Rigidbody2D** — физика движения
- **Collider2D** — обнаружение столкновений
- **Tilemap** — генерация уровней
- **TilemapCollider2D** — коллизии тайлмапа
- **CompositeCollider2D** — оптимизация коллизий

### Рекомендации по настройке:
1. **Physics2D Settings** — оставить по умолчанию
2. **Input Manager** — использовать стандартные оси Horizontal/Vertical
3. **Tags** — создать тег "Wall" для стен
4. **Layers** — пока используем Default, позже создадим слои для игрока/врагов/окружения

### Производительность:
- `CompositeCollider2D` объединяет множество тайлов в один коллайдер
- `FixedUpdate()` используется для физики
- `GetAxisRaw()` обеспечивает мгновенный отклик без сглаживания

---

*Документация обновлена: после реализации базового движения и генерации уровней* 